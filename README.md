# cloud-resume-portfolio
Personal cloud resume and portfolio website hosted on AWS. Static frontend in HTML/CSS/JS on S3 + CloudFront, with a serverless backend (API Gateway, Lambda, DynamoDB) for visitor analytics and contact form, fully automated via GitHub Actions and IAM OIDC.
